[pytest]
# Make the project root importable (so `import src.nn...` resolves) and
# discover tests under tests/. pytest is a TEST framework, not an ML framework:
# it does not affect the Type A nature of the project.
pythonpath = .
testpaths = tests
addopts = -ra
