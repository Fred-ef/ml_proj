"""Core neural-network components: layers, network, activations, losses,
optimizers, regularizers and weight initializers.

Everything here is implemented from scratch on top of NumPy only (Type A
requirement): no autograd, no high-level NN frameworks.
"""
